/* const menu = {
  seletor: '.principal',
};
console.log(menu.seletor);
 */

/* function showList(empresa, ...clientes) {
  clientes.forEach((cliente) => {
    console.log(cliente, empresa);
  });
}
showList('google', 'Bruno', 'Felipe'); */

/* const numeros = [10, 5, 60]
const maior = Math.max(...numeros);
console.log(maior); */

const precos = [
  'Créditos',
  'R$ 200',
  'R$ 400',
  'Contas a Pagar',
  'R$ 300',
  'R$ 400',
  'Meus Dados',
];

/* const arrayPreco = precos.filter((preco) => preco.includes('R$ '));
const numerosLimpo = arrayPreco.map((preco) => Number(preco.replace("R$ ","")));
console.log(numerosLimpo); */

